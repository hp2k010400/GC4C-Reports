name: Weekly Grip Email

on:
  schedule:
    - cron: '0 5 * * 1'  # 6am BST (5am UTC) every Monday

concurrency:
  group: weekly-grip-email
  cancel-in-progress: false

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: Send grip email
        run: |
          curl -f "https://gc4creportsandstock.netlify.app/.netlify/functions/trigger-grip-email-background?secret=gc4c-test-2026"

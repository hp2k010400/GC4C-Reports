name: Daily POS Email

on:
  schedule:
    - cron: '0 18 * * *'  # 7pm BST (6pm UTC) every day

concurrency:
  group: daily-pos-email
  cancel-in-progress: false

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: Send POS email
        run: |
          curl -f "https://gc4creportsandstock.netlify.app/.netlify/functions/trigger-pos-email-background?secret=gc4c-test-2026"

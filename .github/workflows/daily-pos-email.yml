name: Daily POS Email

on:
  schedule:
    - cron: '0 18 * * *'  # 7pm BST (6pm UTC) every day
  workflow_dispatch:       # allow manual trigger from GitHub UI

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: Send POS email
        run: |
          curl -f "https://gc4creportsandstock.netlify.app/api/test-pos-email?secret=gc4c-test-2026"
